# Daily Standup — 09/07/2026

## ¿Qué hicimos ayer?

- **T0.10 / SL-10 ✅ COMPLETADA** — Mock de contratos Flutter (spec §6) implementado y verificado.

  Se crearon los siguientes servicios Dart en `Frontend/lib/services/`, cada uno con `_useMock = true` que implementa exactamente los contratos de `2_spec.md §6`:

  | Servicio | Contratos cubiertos |
  |---|---|
  | `auth_service.dart` | POST /api/v1/auth/register · login · refresh |
  | `monitored_service.dart` | GET/POST/DELETE /api/v1/monitored-persons + consent |
  | `telemetry_service.dart` | POST /api/v1/telemetry/windows · GET /status/{id} |
  | `alerts_service.dart` | GET/PATCH /api/v1/alerts |
  | `devices_service.dart` | POST /api/v1/devices/pair · push-token |
  | `admin_service.dart` | GET /admin/history · export · users · retrain |

  Modelos Dart creados: `User`, `MonitoredPerson`, `Alert`, `RetrainJobStatus` + excepciones compartidas en `exceptions.dart`.

  **32 tests unitarios** pasando en `Frontend/test/mock_services_test.dart` — cubren happy paths y casos de error de todos los contratos.

- **T0.2 / SL-8 ✅ COMPLETADA** — Renombrado completo de la app a **SentiLife** (`com.sentilife.app`) en todas las plataformas (Android, iOS, Web, Windows, Linux, macOS). 
  - Nueva app registrada en Firebase con package ID `com.sentilife.app`
  - Secrets de GitHub actualizados: `GOOGLE_SERVICES_JSON` y `FIREBASE_APP_ID`
  - PR `feature/rename-sentilife` → `dev` abierto

## ¿Qué hicimos hoy?

- **T0.4 / SL-2 ✅ COMPLETADA** — Estructura completa `backend-java/` creada desde cero:
  
  **Stack:** Spring Boot 3.3.2 + Java 21 + Maven
  
  **Estructura de paquetes** `com.sentilife.*`:
  - `auth/` — autenticación JWT
  - `users/` — gestión usuarios
  - `monitored/` — personas monitorizadas
  - `consent/` — consentimiento GDPR
  - `telemetry/` — ingesta ventanas
  - `alerts/` — alertas y feedback
  - `notifications/` — push FCM
  - `admin/` — historial, export, retrain
  - `ota/` — actualización OTA
  - `config/` — Security, RabbitMQ
  
  **Dependencias en `pom.xml`:**
  - spring-boot-starter-web
  - spring-boot-starter-security + jjwt (JWT)
  - spring-boot-starter-data-jpa + postgresql
  - flyway-core + flyway-database-postgresql
  - spring-boot-starter-validation
  - spring-boot-starter-actuator + micrometer-registry-prometheus
  - spring-boot-starter-amqp (RabbitMQ)
  - lombok + H2 (test)
  
  **Configuración:**
  - `application.yml` — base local
  - `application-docker.yml` — perfil Docker con servicios en red interna
  - `application-test.yml` — H2 en memoria para tests sin PostgreSQL
  
  **Seguridad base:** `/actuator/health` público, JWT stateless preparado
  
  **Docker:** Dockerfile multi-stage (build con Maven → runtime JRE alpine), health check incluido
  
  **Infraestructura actualizada:**
  - `docker-compose.yml` — añadido servicio `backend-java` + `rabbitmq` con management UI
  - `.env.example` — nuevas variables: `JAVA_PORT`, `RABBITMQ_USER/PASSWORD`, `JWT_SECRET`, `INFERENCE_URL`
  
  **Tests:** `SentiLifeApplicationTests` — verifica que el contexto de Spring arranca sin errores
  
  **Estado:** `/actuator/health` funcional cuando se levante el compose. Pendiente: migraciones Flyway (SL-3), endpoints de negocio (Fase 2+).

## ¿Qué vamos a hacer mañana?

- SL-3 — Flyway + seed (tablas spec §5.1 + usuario IT_ADMIN)
- SL-13 — EDA SisFall (ruta crítica ML)
- Verificar T0.INT — `docker compose up` levanta todo el stack con health checks verdes

## Impedimentos

- Ninguno.
