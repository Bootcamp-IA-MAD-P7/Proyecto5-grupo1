<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug: permite HTTP a API local (docker-compose en LAN). -->
    <application android:usesCleartextTraffic="true" />
</manifest>
